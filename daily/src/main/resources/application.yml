server:
 port: 8006
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/student_db?useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: Gandham@123
    hikari:
      maximum-pool-size: 2
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect